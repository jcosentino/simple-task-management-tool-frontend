// This test is commented because of a conflict between .ts test files and the linter

// describe('Task tests', () => {
//   it('make sure that tests are visible', async () => {
//     expect(1).toEqual(1);
//   });
// });
